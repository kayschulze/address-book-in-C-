@{
  Layout = "_Layout";
}

<div class="container">
  <h1>Places You've Been</h1>
  <ul>
      @* <h3>@Model.GetID(). @Model.GetLocation()</h3>
      <li>Year: @Model.GetYear()</li>
      <li>Season: @Model.GetSeason()</li>
      <li>Favorite Attraction: @Model.GetFavoriteAttraction()</li>
      <li>Comments: @Model.GetComments()</li> *@

      <button><a href="/contact/add">Add Contact</a></button>
      <button><a href="contacts/clear">Clear Contacts</a></button>
      <button><a href="/">Return to Homepage</a></button>
</div>
