@{
  Layout = "_Layout";
}

<div class="container">
  <h1>Places You've Been</h1>
  <ul>
    @* @foreach (var place in Model)
    {
      <h3>@place.GetID(). @place.GetLocation()</h3>
      <li>Year: @place.GetYear()</li>
      <li>Season: @place.GetSeason()</li>
      <li>Favorite Attraction: @place.GetFavoriteAttraction()</li>
      <li>Comments: @place.GetComments()</li>
    } *@

    <button><a href="/contact/add">Add Contact</a></button>
    <button><a href="contacts/clear">Clear Contacts</a></button>
    <button><a href="/">Return to Homepage</a></button>
</div>
